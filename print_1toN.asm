.MODEL SMALL
.STACK 100H
.DATA
    N CX 5
.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX

    MOV CX, 5        ; Print 1 to 5
    MOV DL, '1'

PRINT_LOOP:
    MOV AH, 02H
    INT 21H
    INC DL
    LOOP PRINT_LOOP

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN
